export enum QTLongImageOrientation {
  ORIENTATION_USE_EXIF = -1,
  /**
   * 以原始方向显示图像文件。
   */
  ORIENTATION_0 = 0,
  /**
   * 将图像顺时针旋转90度。
   */
  ORIENTATION_90 = 90,
  /**
   * 将图像顺时针旋转18度。
   */
  ORIENTATION_180 = 180,
  /**
   * 将图像顺时针旋转270度。
   */
  ORIENTATION_270 = 270,
}
